# monthly-report

基于 GitHub Commits 自动生成月报的 TypeScript + Node.js 工具

## 功能特性

- ✅ 从 GitHub 仓库获取指定时间范围内的 commits
- ✅ 支持多个仓库和私有仓库
- ✅ 支持为每个仓库指定不同分支
- ✅ 自动过滤 Merge 类型的提交
- ✅ 使用 DeepSeek AI 自动生成专业的月报
- ✅ 支持自定义月报模板
- ✅ 生成的月报可直接复制到邮件中使用

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env`:

```bash
cp .env.example .env
```

编辑 `.env` 文件,填入你的配置:

```env
# GitHub Token (访问私有仓库时必需)
GITHUB_TOKEN=ghp_your_token_here

# GitHub 作者名称
GITHUB_AUTHOR=your_github_username

# GitHub 仓库列表(逗号分隔)
# 支持指定分支: owner/repo:branch
GITHUB_REPOS=owner/repo1:main,owner/repo2:develop,owner/repo3

# 报告时间范围
REPORT_TIME=2025-01-01,2025-01-31

# DeepSeek API Key (用于生成月报)
DEEPSEEK_API_KEY=your_deepseek_api_key

# 部门名称(可选)
DEPARTMENT=Front-end - R&D
```

### 3. 运行

```bash
npm run dev
```

## 配置说明

详细的配置说明请查看 [SETUP.md](./SETUP.md)

### GitHub Token

- **公开仓库**: 可选,但建议配置以提高 API 限制
- **私有仓库**: 必需,需要 `repo` 权限

获取 Token: https://github.com/settings/tokens

### DeepSeek API Key

获取 API Key: https://platform.deepseek.com/api_keys

### 仓库和分支配置

支持为每个仓库指定不同的分支:

```env
# 格式: owner/repo:branch
GITHUB_REPOS=owner/repo1:main,owner/repo2:develop,owner/repo3:feature/new-ui

# 不指定分支则使用默认分支
GITHUB_REPOS=owner/repo1,owner/repo2:develop
```

**使用场景**:
- 不同仓库使用不同的主分支(main/master/develop)
- 需要统计特定功能分支的提交
- 多环境开发(开发分支、测试分支、生产分支)

### Commit 过滤规则

工具会自动过滤以下类型的 Merge 提交:
- `Merge pull request #xxx from ...`
- `Merge branch 'xxx' into ...`
- `Merge remote-tracking branch ...`

这样可以让月报更专注于实际的功能开发和问题修复,避免 Merge 提交的干扰。

### 智能周划分

工具会在代码层面自动按周分组 commits(以周一为起始):
- ✅ 自动识别完整周和不完整周
- ✅ 使用 `date-fns` 的 `isWithinInterval` 精确匹配日期范围
- ✅ 在代码层面完成分组,确保 100% 准确
- ✅ AI 只负责润色内容,不参与日期匹配
- ✅ 每周标题显示具体的日期范围,例如: `第一周 (2025-09-01 到 2025-09-07)`

这样可以确保月报中的周划分与实际日期完全对应,避免 AI 分配错误。

## 使用示例

### 只获取 Commits 列表

不设置 `DEEPSEEK_API_KEY`,只会显示 commits 列表:

```bash
npm run dev
```

### 生成完整月报

设置 `DEEPSEEK_API_KEY` 后,会自动调用 AI 生成月报:

```bash
npm run dev
```

输出示例:

```
✅ 认证成功: your_username
📊 账户类型: User

📊 获取 Commits...

  - 正在获取 owner/repo1 (main) 的提交记录...
    ✅ 找到 25 个提交
  - 正在获取 owner/repo2 的提交记录...
    ✅ 找到 15 个提交

✅ 总共找到 25 个提交

📝 Commits 列表:
...

🤖 正在使用 DeepSeek 生成月报...

================================================================================
📄 生成的月报:
================================================================================

## 部门

Front-end - R&D

## 本月工作总结
...

================================================================================

✅ 月报已生成并保存到: /path/to/project/.output/2025-01.md
💡 可以直接复制到邮件中使用
```

生成的月报会自动保存到 `.output` 目录下,文件名格式为 `YYYY-MM.md`。

## 自定义模板

编辑 `src/template/report.md` 文件来自定义月报模板格式。

## 命令

- `npm run dev` - 开发模式运行
- `npm run build` - 构建项目
- `npm start` - 运行构建后的代码
- `npm run type-check` - 类型检查

## 项目结构

```
src/
├── libs/
│   ├── github.ts      # GitHub API 客户端
│   └── deepseek.ts    # DeepSeek AI 客户端
├── types/
│   ├── github.ts      # GitHub 相关类型
│   ├── deepseek.ts    # DeepSeek 相关类型
│   └── index.ts       # 类型统一导出
├── template/
│   └── report.md      # 月报模板
└── index.ts           # 主入口文件
.output/               # 生成的月报输出目录
└── YYYY-MM.md         # 按月份命名的月报文件
```

## 常见问题

### 404 Not Found 错误

- 确保 GitHub Token 有 `repo` 权限(访问私有仓库)
- 确认你对仓库有访问权限

### DeepSeek API 调用失败

- 检查 API Key 是否正确
- 确认账户有足够的额度

更多问题请查看 [SETUP.md](./SETUP.md)
